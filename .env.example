# Copy to .env and adjust for your environment.
PORT=3000

# MySQL connection (the app connects over TCP)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=tableflip
DB_PASSWORD=tableflip
DB_NAME=tableflip

# The test suite uses its own database so it can be truncated freely.
TEST_DB_NAME=tableflip_test
